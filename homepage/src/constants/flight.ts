/** Typical commercial airline cruise speed used for flight time estimates. */
export const FLIGHT_CRUISE_SPEED_KMH = 800;

/** Time buffer added to raw distance/speed estimate to account for taxi, takeoff, and landing. */
export const FLIGHT_TIME_BUFFER_HRS = 0.5;
